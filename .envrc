use flake .
# Required for conditional compilation to work nice when using dune directly.
export IS_NIX_BUILD_ENV=false;
# Required to make the LSP work in Zed editor.
export NIX_BUILD_TOP="/tmp/this/does/not/exist/but/stops/zed/from/complaining"
